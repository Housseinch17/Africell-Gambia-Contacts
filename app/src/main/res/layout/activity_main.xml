<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!--  Shown when READ_CONTACTS and/or WRITE_CONTACTS permission has not been granted yet  -->
    <LinearLayout
        android:id="@+id/permissionLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/permissionText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:text="@string/permission_rationale"
            android:textSize="16sp" />

        <Button
            android:id="@+id/grantPermissionButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/grant_permission" />

        <!--  Only shown after the permission request is denied — deep-links to this app's
                 page in the system Settings so the user can still grant it manually.  -->
        <Button
            android:id="@+id/openSettingsButton"
            style="?attr/borderlessButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="@string/open_settings"
            android:textAllCaps="false"
            android:visibility="gone" />
    </LinearLayout>

    <!--  Shown only when there's a saved "last run" available to undo (see UndoManager) —
             stays visible across app restarts until the next Apply run replaces it or it's
             undone, so "later I can undo to it" holds even after closing the report screen.  -->
    <LinearLayout
        android:id="@+id/undoBannerLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/report_screen_bg"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/undoBannerText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/text_secondary"
            android:textSize="13sp" />

        <Button
            android:id="@+id/undoLastUpdateButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:background="@drawable/pill_button_outline"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingTop="6dp"
            android:paddingBottom="6dp"
            android:stateListAnimator="@null"
            android:text="@string/undo"
            android:textAllCaps="false"
            android:textColor="@color/accent_purple"
            android:textSize="13sp" />
    </LinearLayout>
    <!--  Step 3, as three tabs: tapping one filters the list below to just that status.
             Tab labels are updated with live counts in MainActivity.updateSummary(). This
             sits right at the top (replacing the old summary bar) so it's the first thing
             visible once contacts are loaded.  -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/statusTabLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:tabGravity="fill"
        app:tabMode="fixed"
        app:tabIndicatorColor="@color/status_changeable_bg"
        app:tabSelectedTextColor="@color/white"
        app:tabTextColor="@color/white_40" />

    <LinearLayout
        android:id="@+id/changeableActionsLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingBottom="4dp"
        android:visibility="gone">

        <CheckBox
            android:id="@+id/selectAllCheckbox"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/select_all" />

        <Button
            android:id="@+id/fixSelectedButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:text="@string/fix_selected"
            android:textAllCaps="false" />

    </LinearLayout>
    <!--  TESTING ONLY — writes/removes a curated set of fake contacts covering every
             classification branch. Remove this row before shipping.  -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingBottom="8dp">

        <Button
            android:id="@+id/seedTestContactsButton"
            style="?attr/borderlessButtonStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="6dp"
            android:layout_weight="1"
            android:text="Add test contacts"
            android:textAllCaps="false" />

        <Button
            android:id="@+id/removeTestContactsButton"
            style="?attr/borderlessButtonStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:layout_weight="1"
            android:text="@string/remove_test_contacts"
            android:textAllCaps="false" />
    </LinearLayout>
    <!--  Shown while contacts are being read from the device (step 1)  -->
    <ProgressBar
        android:id="@+id/contactsProgressBar"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="32dp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/loadingText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:text="@string/loading_contacts"
        android:textColor="@color/text_secondary"
        android:visibility="gone" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/contactsRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:padding="8dp" />
</LinearLayout>